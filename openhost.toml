[app]
name = "backup"
version = "0.1.0"
description = "User-controlled incremental backups of app data via restic"

[runtime]
type = "serverfull"

[runtime.container]
image = "Dockerfile"
port = 8080

[resources]
memory_mb = 2048
cpu_cores = 1.0

[data]
access_all_app_data = true
