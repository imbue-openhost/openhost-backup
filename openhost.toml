[app]
name = "backup"
version = "0.1.0"
description = "User-controlled backups of app data via rclone"

[runtime]
type = "serverfull"

[runtime.container]
image = "Dockerfile"
port = 8080

[resources]
memory_mb = 2048
cpu_millicores = 1000

[data]
access_all_data = true
